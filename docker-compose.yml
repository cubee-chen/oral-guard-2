services:
  frontend:
    build:
      context: ./frontend
    dns:
      - 8.8.8.8
      - 8.8.4.4
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_BASE_URL=http://localhost:5000
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    # environment:
    #   # 指向 Atlas 的 URI
    #   - MONGODB_URI=mongodb+srv://cubee0405:YU9ck80IEOtu3cpk@cluster0.xmzcrfk.mongodb.net/oral-guard?retryWrites=true&w=majority&appName=Cluster0
    #   - FRONTEND_URL=http://localhost:5173
    #   - ML_API_URL=http://ml-service:8000/predict
    env_file:
      - .env
    depends_on:
      - ml-service

  ml-service:
    build:
      context: ./ml-service
    dns:
      - 8.8.8.8
      - 8.8.4.4
    ports:
      - "8000:8000"
    volumes:
      - ./ml-service:/app
      - model_data:/app/models

volumes:
  model_data:
